const User = require('./sequelize/user');

setTimeout(() => {
  (async () => {
    // const user = await User.create({
    //   provider_id: 327894,
    //   provider: 'wechat'
    // });
  })()
}, 3000);